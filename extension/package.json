{
  "name": "ai-affiliate-studio-extension",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.30",
    "typescript": "^5"
  }
}
